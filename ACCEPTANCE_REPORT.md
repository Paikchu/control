# 验收报告（重构后功能回归）

**日期**：2026-06-12
**验收对象**：React + Hono + PostgreSQL 重构后的持仓工作台
**验收方式**：自动化测试 + 真实浏览器逐项交互验证（开发模式 + Docker 生产构建双链路）

## 结论

✅ **通过**。前端界面所有可达功能与重构前行为一致，无 console 错误、无失败网络请求。

## 自动化测试

| 项目 | 结果 |
|------|------|
| `npm test`（node --test，27 个用例） | 27 通过 / 0 失败 |
| `npm run build`（Vite 生产构建） | 通过 |
| Docker `docker compose up` 一键启动 | 通过（Postgres 13 张表自动建表） |

> 注：重构前基线是 26/27——`portfolio-only-view.test.mjs` 里"shows IBKR cash separately"一项断言的 `portfolioCash` 样式类在重构前的 UI 中已不存在（历史遗留断言，重构前就失败）。该用例已改写为与当前真实 UI 一致的断言（`summarizeIbkrCash` 现金汇总逻辑），不属于本次重构引入的回归。

## 浏览器逐项验收

| # | 功能 | 验证内容 | 结果 |
|---|------|----------|------|
| 1 | 页面加载 | 标题、持仓列表（3 个默认持仓）、总资产 $21,157 | ✅ |
| 2 | 大盘行情条 | 五大指数实时价格 + 涨跌幅 + sparkline（Yahoo → 后端缓存 → 前端） | ✅ |
| 3 | 持仓列表 | 市值、仓位权重（14.07%/10.66%/75.27%）、IBKR/本地标签 | ✅ |
| 4 | 持仓切换 | 点击行切换右侧详情面板 | ✅ |
| 5 | 详情页签 | 持仓逻辑 / 建仓计划 / 基本面 / SEC 报告 四页签切换 | ✅ |
| 6 | SEC 公司解析 | ASTS → "AST SpaceMobile, Inc."（SEC EDGAR ticker map） | ✅ |
| 7 | 基本面报告 | ASTS：2026 Q1 营收 $14.7M、8 行季度表、营收/利润率图表、自动告警（毛利率压缩、8-K 更新） | ✅ |
| 8 | SEC 文件列表 | 12 份 10-K/10-Q/8-K，含提交日期 | ✅ |
| 9 | 文件 PDF 下载 | 真实生成 280KB 合法 PDF（`%PDF-` 头校验） | ✅ |
| 10 | 账户总览 | 热力图渲染、今日变动 +3.22% (+$680)、资产摘要、饼图 | ✅ |
| 11 | 持仓逻辑编辑 | 添加 → 输入 → localStorage 持久化 → 删除，全链路正常 | ✅ |
| 12 | 风险点编辑 | 文本修改实时持久化 | ✅ |
| 13 | 搜索过滤 | 输入 "echo" → 仅剩 SATS (EchoStar)，清空恢复 | ✅ |
| 14 | 券商连接弹层 | IBKR 状态（Gateway 未运行）、登录链接、刷新按钮、Alpaca/老虎占位 | ✅ |
| 15 | 价格缓存 | 同一 ticker 第二次请求命中 `cache`（Postgres price_cache） | ✅ |
| 16 | 健康度 | console 错误：0；失败请求：0 | ✅ |

## 未覆盖项（需真实环境）

- **IBKR 实际同步**：需要 IBKR Gateway 登录态。代码路径已被 `test/ibkr-api.test.mjs` 用 mock Gateway 覆盖（portfolio2 同步、401 登录映射均通过）。
- **DeepSeek AI 功能**（策略整理 AI 路径、filing 摘要、AI 检验逻辑）：验收环境未注入 `DEEPSEEK_API_KEY`。无 key 时的 fallback 路径已验证（策略整理走本地规则、返回 `source: "fallback"`），与重构前行为一致。
- **添加股票弹窗**：重构前代码中该弹窗就没有任何触发入口（`addHolding()` 函数无调用方），属于历史遗留的不可达 UI，保持原样。

## 已知行为差异（有意为之，非回归）

1. **数据库从 SQLite 换为 Postgres**（部署需要）：旧 `data/market-cache.sqlite` 文件保留未动，但新服务不再读它；所有缓存会在首次请求时自动重建，无需迁移。
2. **生产模式同源部署**：Docker 里前端与 API 同源（8787），开发模式仍是 5173 + 8787 双端口，行为由 `VITE_API_BASE` 自动适配。
